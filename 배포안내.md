# 원장님 AI — 배포 안내

**설치가 끝났습니다.** 아래는 지금 상태와, 앞으로 손볼 때 보는 안내입니다.

## 지금 살아 있는 것

| | |
|---|---|
| **사이트** | https://hoho70200-prog.github.io/wonjang-ai/ |
| **대시보드** | https://hoho70200-prog.github.io/wonjang-ai/admin.html |
| **저장소** | https://github.com/hoho70200-prog/wonjang-ai |
| **데이터베이스** | Supabase · 프로젝트 `arsnfylagakrjgmekkbx` (서울) |

- 1부만 열려 있고 2~6부는 "준비 중". **잠긴 부의 원고는 서버에 아예 없습니다.**
- 원장님들은 로그인 없이 링크만 누르면 읽습니다.
- 통계는 hoho70200@gmail.com 계정으로만 볼 수 있고, **신규 가입은 막아 뒀습니다.**
- 실습에 적은 내용은 원장님 기기에만 남습니다.

> ⚠️ **anon 키는 GitHub의 `assets/app.js`에만 들어 있습니다.**
> 이 폴더의 app.js에는 `여기에_붙여넣기` 자리표시자가 그대로입니다.
> **로컬 app.js를 고쳐서 다시 올리면 키가 지워집니다.** 올리기 전에 키를 다시 넣으십시오.
> (키는 Supabase → Settings → API Keys → Legacy → `anon public`)
>
> 키가 자리표시자로 남은 채 올라가면 통계가 **한 건도** 안 쌓입니다.
> 그런 상태로 대시보드를 열면 이제 "아직 연결 전입니다" 화면이 뜹니다.
> (예전에는 로그인 화면이 떠서, 키가 빠진 줄 모르고 지나칠 수 있었습니다.)

---

## 지금 손보고 올릴 때 — 순서 (2026-07-27 개정)

**① Supabase → SQL Editor에 `supabase.sql`을 통째로 붙여넣고 실행.**
표 구조는 그대로라 쌓인 자료는 안 없어집니다. 통계 함수만 새로 깔립니다.

**② `assets/app.js` 맨 위에 anon 키를 다시 넣기.**

**③ HTML 네 개 + `assets/app.js`를 GitHub에 올리기.**

②③을 ①보다 먼저 해도 자료는 안 깨집니다. 다만 새 함수가 깔리기 전까지
대시보드의 "머문 시간" 자리가 비어 보입니다.

---

## 내가 검수한 발자국을 통계에서 빼기

시험 삼아 들어간 내 기록이 원장님들 숫자에 섞이면 완독률이 통째로 흔들립니다.

```
https://hoho70200-prog.github.io/wonjang-ai/?tester=1
```

브라우저(폰·PC 각각)에서 이 주소를 **한 번만** 열면, 그 뒤로 그 브라우저가
하는 일은 서버로 가지 않습니다. 화면 왼쪽 아래에 "통계 제외 중 · d…" 딱지가
조용히 뜹니다. 딱지에 적힌 것이 그 브라우저의 기기 표입니다.
풀 때는 `?tester=0`.

**이미 쌓인 내 기록을 지우려면** Supabase SQL Editor에서 (기기 표는 위 딱지에서 확인):

```sql
delete from public.events   where device = '여기에_기기표';
delete from public.feedback where device = '여기에_기기표';
```

아직 아무에게도 안 돌리셨다면 `truncate public.events;` 로 통째로 비워도 됩니다.

---

## 읽는 구조 — 8단계

원고의 **짝꿍 표기**를 그대로 읽어 소설·개념서·실습을 한 묶음으로 엮습니다.
묶는 규칙을 따로 정하지 않으니, 실습서의 짝꿍 줄만 정확하면 됩니다.

| 단계 | 소설 | 개념서 | 실습 |
|---|---|---|---|
| 1 책상을 차리다 | 1장 | — | 0 |
| 2 정체를 알다 | 2장 | 1장 | 1 |
| 3 탁자를 다루다 | 3장 | 2장 | 2 |
| 4 거짓말을 잡다 | 4장 | 3장 | 3 |
| 5 역할을 주다 | 5장 | 4장 | 4 |
| 6 뼈대를 세우다 | 6·7장 | 5장 | 5 |
| 7 말투를 입히다 | 8·9장 | 6장 | 6 |
| 8 비품을 세다 | 10장 | — | 마무리 |

단계 이름은 `assets/practice.json`의 각 실습에 있는 `"stage"` 값입니다. 고치면 바로 반영됩니다.

원장님은 목차 위에서 **단계로 보기**(기본)와 **소설만 이어 읽기** 중에 고를 수 있고,
고른 대로 각 장 끝의 "다음으로"도 따라갑니다.

---

## 확인된 것

- 익명 쓰기 201, **익명 읽기는 차단**(빈 배열) — 소스에서 키를 봐도 데이터는 못 가져갑니다
- 소설·개념서·실습 페이지 정상, 부탁 조립과 복사 버튼 동작 확인
- 자동 점검 60건 통과 (`node tools/test_site.js`)

---

## 대시보드에서 보이는 것

| 화면 | 뜻 |
|---|---|
| 들어온 사람 | 링크를 실제로 연 기기 수. **업무연락 수신확인과 전혀 다른 숫자입니다** |
| 한 장이라도 끝낸 사람 | 소설 **아무 한 장**이라도 끝까지 본 사람 |
| **소설을 다 읽은 사람** | **열려 있는 소설 10장을 전부 끝낸 사람 = 진짜 완독** |
| **부탁을 복사한 사람** | **실제로 AI를 써 봤다는 가장 강한 신호** |
| 얼마나 머무셨나 | 소설·개념서·실습별 평균·가운뎃값·최장·최단 |
| 장별로 얼마나 머무셨나 | 장마다 머문 시간과 **예상 대비 %**. 40% 아래·200% 위는 주황색 |
| 얼마나 내려 읽었나 | 장을 한 번 연 것을 하나로 세어, 어디까지 내려갔나 |
| 어디서 멈추나 | 장별 완료율. 40% 아래는 주황색 — 그 장에 문제가 있다는 뜻 |
| 제일 오래 붙잡은 서류 | 원장님들이 직접 적어 주신 것. **서비스의 재료입니다** |
| 막혔던 곳 | 자유 피드백 원문 |

### "다 읽었다"를 어떻게 세는가

두 가지가 **함께** 맞아야 한 장을 끝낸 것으로 칩니다.

1. 그 장의 맨 아래(95% 이상)까지 내려갔을 것
2. 그 장을 읽을 만한 시간을 실제로 머물렀을 것
   — 예상 읽는 시간의 40%, 짧아도 45초, 길어도 4분

7분짜리 소설 1장이면 168초입니다. 예전에는 **20초**만 넘으면 됐고,
화면보다 짧은 장은 스크롤조차 없이 완독으로 잡혔습니다.
그리고 소설 한 장만 끝내도 "끝까지 읽은 사람"에 들어갔습니다. 둘 다 고쳤습니다.

### 머문 시간은 어떻게 재는가

화면을 **보고 있는 동안만** 셉니다. 다른 탭·다른 앱으로 넘어가면 시계가 멈추고,
90초 넘게 아무 움직임(스크롤·터치·키·마우스)이 없어도 멈춥니다.
켜 놓고 자리를 뜬 시간이 "세 시간 읽음"으로 둔갑하지 않게 하려는 것입니다.

한 번 열 때마다 15초·1분·2분… 하고 여러 번 보고하되, 열람마다 붙는 표로
가장 큰 값 하나만 세므로 같은 시간이 겹쳐 세어지지 않습니다.
5초 미만은 잘못 눌러 들어온 것으로 보고 아예 안 셉니다.

---

## 원고를 고쳤을 때

docx를 수정하신 뒤 이 폴더에서:

```
pip install python-docx
python3 tools/build_content.py
```

바뀐 원고가 사이트에 그대로 반영됩니다. 손으로 옮길 일은 없습니다.
단계 구성과 자료 판 번호도 이때 같이 다시 계산됩니다.

> **판 번호가 왜 필요한가** — 파일만 바꿔 올리면, 이미 다녀간 원장님의 브라우저는
> 예전 파일을 그대로 씁니다. 이 스크립트가 `app.js?v=…` 뒤 번호를 매번 바꿔 주므로
> 저절로 새 파일을 받습니다. **고친 뒤에는 HTML 네 개도 같이 올리십시오.**

**2부를 열 때**는 `tools/build_content.py` 위쪽의 이 줄을 고치고 다시 돌리십시오.

```python
OPEN_PARTS = [1, 2]
```

그러면 2부 원고 파일이 그때 만들어집니다. 그전까지는 서버에 없습니다.

실습은 자동 변환이 안 됩니다 — 빈칸과 부탁 조립을 사람이 설계해야 해서요.
`assets/practice.json`에 1편 형식 그대로 이어 쓰면 됩니다.

---

## 고칠 곳이 생기면

```
node tools/test_site.js
```

64가지를 자동으로 눌러 보고 깨진 곳을 알려 줍니다.
(처음 한 번만 `npm install jsdom`)

---

## 파일 지도

```
index.html          목차 · 진도 · 피드백
read.html           소설·개념서 읽기
practice.html       실습 (빈칸 → 부탁 조립 → 복사)
admin.html          대시보드 (나만)
supabase.sql        데이터베이스 설치 — 한 번만 실행
assets/
  app.js            ← 설정은 여기 맨 위
  style.css
  practice.json     실습 정의 (손으로 씀)
  content/          원고 (자동 생성)
tools/
  build_content.py  원고 → 웹
  test_site.js      자동 점검
```

---

## 챙겨 둘 것

**개인정보** — 원장님 이름·이메일을 받지 않습니다. 기기마다 무작위 글자 하나가
생길 뿐입니다. 어린이집 이름은 선택 입력이고, 실습에 적은 내용은 서버로 가지 않습니다.
개인 프로젝트로 개인정보를 모으기 시작하면 나중에 곤란해지니, 이 선은 지키시는 것이 좋습니다.

**개인 프로젝트와 업무의 경계** — 업무연락으로 나가면 받는 쪽은 "시가 배포한 것"으로
인식합니다. 나중에 서비스 얘기가 나올 때 애매해지니, **연합회를 주 경로로,
업무연락을 보조로** 두시는 편이 안전합니다.

**Supabase 무료 계정**은 일주일 넘게 아무도 안 들어오면 잠깐 멈춥니다.
다시 들어오면 깨어나지만, 그 사이 이벤트는 몇 건 놓칠 수 있습니다.
