# 원장님 AI — 배포 안내

원고를 웹으로 옮기고, 누가 어디까지 읽는지 볼 수 있게 하는 한 벌입니다.
**돈은 한 푼도 들지 않습니다.**

---

## 지금 상태

- **1부만 열려 있고** 2~6부는 목차에 "준비 중"으로만 보입니다.
  잠긴 부의 **원고는 아예 올라가지 않습니다** — 주소를 알아도 미리 읽을 수 없습니다.
- 원장님들은 **로그인 없이** 링크를 누르면 바로 읽습니다.
- 통계는 **호호홍 계정으로만** 볼 수 있습니다.
- 실습에 적으신 내용은 **원장님 기기에만** 남습니다. 서버로 올라가지 않습니다.

---

## 1단계 · 그냥 열어 보기 (지금 바로)

`index.html`을 더블클릭하면… **안 됩니다.** 브라우저 보안 때문에 원고 파일을 못 읽어요.
확인만 하시려면 이 폴더에서 아래를 실행하십시오.

```
python3 -m http.server 8000
```

그리고 브라우저에서 `http://localhost:8000` 을 엽니다.
끄실 때는 터미널에서 Ctrl+C.

---

## 2단계 · 인터넷에 올리기 (10분, 무료)

### GitHub Pages

1. github.com 가입 → **New repository** → 이름은 아무거나 (예: `wonjang-ai`) → **Public**
2. 이 폴더의 파일을 **통째로** 끌어다 올립니다 (`tools` 폴더는 빼도 됩니다)
3. 저장소 → **Settings → Pages** → Source를 `main` / `(root)` 로 두고 저장
4. 1~2분 뒤 `https://아이디.github.io/wonjang-ai/` 로 열립니다

### 더 쉬운 쪽 — Netlify Drop

[app.netlify.com/drop](https://app.netlify.com/drop) 에 폴더를 끌어다 놓으면 주소가 바로 나옵니다.
가입도 나중에 하셔도 됩니다.

> **주소는 짧고 읽히는 것이 좋습니다.** 업무연락에 낯선 주소가 있으면 원장님들이
> 피싱으로 의심합니다. 도메인을 하나 사시면(연 1~2만 원) 클릭률이 눈에 띄게 달라집니다.

---

## 3단계 · 통계 켜기 (20분, 무료)

### 3-1. Supabase 프로젝트 만들기

1. [supabase.com](https://supabase.com) 가입 → **New project**
2. 이름 아무거나, 지역은 **Northeast Asia (Seoul)**
3. 데이터베이스 비밀번호는 적어 두십시오

### 3-2. 표 만들기

프로젝트 → 왼쪽 **SQL Editor** → **New query** →
이 폴더의 **`supabase.sql`** 을 통째로 붙여넣고 **Run**.

한 번만 하면 됩니다.

### 3-3. 열쇠 두 개 옮기기

프로젝트 → **Settings → API** 에서 두 가지를 복사합니다.

- **Project URL** (`https://xxxx.supabase.co`)
- **anon public** 키 (긴 글자)

`assets/app.js` 맨 위를 이렇게 채웁니다.

```js
const CONFIG = {
  SUPABASE_URL: "https://xxxx.supabase.co",
  SUPABASE_ANON_KEY: "eyJhbGci....",
  SITE: "wonjang-ai"
};
```

> **anon 키는 공개되어도 되는 키입니다.** `supabase.sql`이 걸어 둔 규칙 덕분에
> 이 키로는 **쓰기만** 되고 **읽기는 안 됩니다.** 남이 소스를 봐도 데이터를 못 가져갑니다.
> 절대 옮기면 안 되는 것은 `service_role` 키입니다. 그건 건드리지 마십시오.

### 3-4. 내 계정 만들기

1. Supabase → **Authentication → Users → Add user**
   → 본인 이메일과 비밀번호. **Auto Confirm User**를 켜십시오.
2. **Authentication → Providers → Email** 에서
   **Enable email signup을 끄십시오.** 이러면 아무도 새로 가입할 수 없습니다.

이제 `주소/admin.html` 에서 그 이메일로 들어가면 대시보드가 보입니다.

---

## 대시보드에서 보이는 것

| 화면 | 뜻 |
|---|---|
| 들어온 사람 | 링크를 실제로 연 기기 수. **업무연락 수신확인과 전혀 다른 숫자입니다** |
| 끝까지 읽은 사람 | 맨 아래까지 내려가고 20초 이상 머문 사람 |
| **부탁을 복사한 사람** | **실제로 AI를 써 봤다는 가장 강한 신호** |
| 어디서 멈추나 | 장별 완료율. 40% 아래는 주황색 — 그 장에 문제가 있다는 뜻 |
| 제일 오래 붙잡은 서류 | 원장님들이 직접 적어 주신 것. **서비스의 재료입니다** |
| 막혔던 곳 | 자유 피드백 원문 |

---

## 원고를 고쳤을 때

docx를 수정하신 뒤 이 폴더에서:

```
pip install python-docx
python3 tools/build_content.py
```

바뀐 원고가 사이트에 그대로 반영됩니다. 손으로 옮길 일은 없습니다.

**2부를 열 때**는 `tools/build_content.py` 위쪽의 이 줄을 고치고 다시 돌리십시오.

```python
OPEN_PARTS = [1, 2]
```

그러면 2부 원고 파일이 그때 만들어집니다. 그전까지는 서버에 없습니다.

실습은 자동 변환이 안 됩니다 — 빈칸과 부탁 조립을 사람이 설계해야 해서요.
`assets/practice.json`에 1편 형식 그대로 이어 쓰면 됩니다.

---

## 고칠 곳이 생기면

```
node tools/test_site.js
```

64가지를 자동으로 눌러 보고 깨진 곳을 알려 줍니다.
(처음 한 번만 `npm install jsdom`)

---

## 파일 지도

```
index.html          목차 · 진도 · 피드백
read.html           소설·개념서 읽기
practice.html       실습 (빈칸 → 부탁 조립 → 복사)
admin.html          대시보드 (나만)
supabase.sql        데이터베이스 설치 — 한 번만 실행
assets/
  app.js            ← 설정은 여기 맨 위
  style.css
  practice.json     실습 정의 (손으로 씀)
  content/          원고 (자동 생성)
tools/
  build_content.py  원고 → 웹
  test_site.js      자동 점검
```

---

## 챙겨 둘 것

**개인정보** — 원장님 이름·이메일을 받지 않습니다. 기기마다 무작위 글자 하나가
생길 뿐입니다. 어린이집 이름은 선택 입력이고, 실습에 적은 내용은 서버로 가지 않습니다.
개인 프로젝트로 개인정보를 모으기 시작하면 나중에 곤란해지니, 이 선은 지키시는 것이 좋습니다.

**개인 프로젝트와 업무의 경계** — 업무연락으로 나가면 받는 쪽은 "시가 배포한 것"으로
인식합니다. 나중에 서비스 얘기가 나올 때 애매해지니, **연합회를 주 경로로,
업무연락을 보조로** 두시는 편이 안전합니다.

**Supabase 무료 계정**은 일주일 넘게 아무도 안 들어오면 잠깐 멈춥니다.
다시 들어오면 깨어나지만, 그 사이 이벤트는 몇 건 놓칠 수 있습니다.
